# Testing to see if this will add aliasrc entries into vim shell.

#[ -f "$HOME/.config/zsh/aliasrc" ] && source "$HOME/.config/zsh/aliasrc"

# Adding this because I want to change the directory of the zsh dotfiles to the ~/.config/zsh directory.

